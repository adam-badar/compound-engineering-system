# Delta Plans

Delta plans capture material scope changes discovered during epic implementation.

Use `/workflows:epic-delta-loop` to generate and approve delta plans, then merge decisions back into parent epic plan and tracker.

Naming:

- `YYYY-MM-DD-<epic-slug>-delta-<topic>.md`

