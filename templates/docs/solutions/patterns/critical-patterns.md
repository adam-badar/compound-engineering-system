# Critical Patterns

High-signal rules that planners/reviewers must check by default.

Add only patterns that are:

- repeatedly useful across epics, or
- high-risk if missed (correctness/security/data integrity/major UX regressions).

Use this format:

## Pattern: <short name>
- **When it applies**:
- **Required guardrail**:
- **Required test/evidence**:
- **Common failure if skipped**:
- **Related docs/PRs**:
